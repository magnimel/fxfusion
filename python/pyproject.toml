[build-system]
requires = ["setuptools >= 77.0.3"]
build-backend = "setuptools.build_meta"

[project]
name = "fxfusion"
version = "0.0.1"
dependencies = ["torch", "flatbuffers"]
requires-python = ">= 3.10"

[tool.setuptools.packages.find]
where = ["."]